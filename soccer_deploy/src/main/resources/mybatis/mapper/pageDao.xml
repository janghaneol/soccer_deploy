<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="soccer.deploy.dao.pageDAO">	
	<select id="view_page_num" resultType="soccer.deploy.dto.pageDTO" parameterType="int">
	<![CDATA[
	SELECT distinct(p.pagenum) FROM notice n, pages p, clickpagenum c WHERE c.startclicknum=1 AND p.pagenum Between c.startclicknum AND c.endclicknum
	AND n.notice_index BETWEEN p.startnum AND p.endnum Order by p.pagenum
 	]]>
	</select>
</mapper>